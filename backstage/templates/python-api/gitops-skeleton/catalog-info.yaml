apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.appName }}
  description: ${{ values.appName }} service
  tags:
    - kubernetes
    - gitops
spec:
  type: service
  lifecycle: production
  owner: platform
