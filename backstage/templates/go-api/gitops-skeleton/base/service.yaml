apiVersion: v1
kind: Service
metadata:
  name: ${{ values.appName }}
  labels:
    app: ${{ values.appName }}
spec:
  selector:
    app: ${{ values.appName }}
  ports:
  - name: http
    port: ${{ values.port }}
    targetPort: ${{ values.port }}
